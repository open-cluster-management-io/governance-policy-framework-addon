apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-template-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      name: policy-template-sync
  template:
    metadata:
      labels:
        name: policy-template-sync
    spec:
      serviceAccountName: policy-template-sync
      containers:
        - name: policy-template-sync
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - policy-template-sync
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "policy-template-sync"
